<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorAccentArkanoid">

    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/colorAccentPrimaryDark"
                android:endColor="@color/colorAccentPrimary"/>
            <stroke
                android:width="@dimen/btnStrokeWidth"
                android:color="@color/colorAccentPrimaryDarkWithAlpha"/>
            <corners
                android:radius="@dimen/btnCornerRadius"/>
        </shape>
    </item>
</ripple>